name: Build Docker Image

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: fribau.champ@gmail.com
          password: $U_2Eu.AyWz3is#$U_2Eu.AyWz3is#

      - name: Build image
        run: docker build -t myuser/pingpong:latest .

      - name: Push image
        run: docker push myuser/pingpong:latest
